# CLAUDE.md

## 프로젝트

개인 홈페이지. 정적 사이트이며 HTML 파일은 여섯 개다.

- `index.html` — 대문. 큰 "824"와 Profile / Community 링크가 중심이다.
  진입 시 양옆으로 열리는 장막과 숫자 등장 애니메이션을 사용한다.
- `profile.html` — 개인 소개. 왼쪽 목차 없이 소개·관심사·이력·글을 순서대로 내려 읽는다.
  기존 섹션 주소인 #intro, #interests, #history, #posts는 유지한다.
- `community.html` — 커뮤니티 대문. 화면을 Board / Ratings / Guestbook의 큰 입구 세 개로 나눈다.
  PC에서는 가로로 나란히, 모바일에서는 세로로 쌓이며 구역 전체가 각 페이지로 이동하는 링크다.
- `board.html` — 게시판. 게시글 목록과 작성 폼이 있다.
- `ratings.html` — 평점. 항목별 별점과 댓글을 남길 수 있다.
- `guestbook.html` — 방명록. 짧은 인사와 메모를 남긴다.
  각 상세 페이지는 자신의 Firebase 기능만 불러온다. A/B/C 탭은 사용하지 않는다.

사이트 문구는 전부 영어다.

## 답변 방식

- 항상 한국어로 답변한다. 코드 주석도 한국어로 작성한다.
- 사용자는 코드를 직접 읽거나 수정하지 못한다. 전문 용어를 최소화하고,
  무엇을 왜 바꿨는지 항상 짧게 요약해준다.

## 기술 제약

- 위 여섯 HTML 파일 안에 CSS와 JS를 유지한다. 별도 파일로 분리하지 않는다.
- 프레임워크와 빌드 도구를 도입하지 않는다. React, Next.js, npm, Vite 설치 금지.
  외부 라이브러리는 CDN `<script>` 태그로만 추가하고, 추가 전에 이유를 설명한다.
- **색은 흑백만 쓴다.** 강조는 색이 아니라 반전(검정 바탕/흰 글씨), 굵기, 크기로 만든다.
  색상은 `:root`의 CSS 변수로만 지정하고 하드코딩하지 않는다.
  변경 시 라이트/다크 양쪽에서 읽히는지 확인한다.
- 폰트는 두 종류다. 본문·제목은 Pretendard, 숫자·라벨·날짜는 `--font-mono`(JetBrains Mono).
  둘 다 CDN `<link>`로 불러온다.
- **색상 변수와 폰트는 모든 페이지에서 동일하게 유지한다.**
  마우스를 따라다니는 원이나 커스텀 커서는 사용하지 않는다.
  장막과 숫자 등장 효과는 대문에만 적용하며 모션 감소 설정에서는 생략한다.
  소개와 커뮤니티에는 왼쪽 고정 패널이나 숫자·알파벳 목차를 사용하지 않는다.
  상단의 간단한 페이지 이동 링크와 하단 연락처는 같은 디자인을 유지한다.
  한 파일을 고치면 다른 파일도 같이 고치고, 그 사실을 사용자에게 알린다.
- 작은 글씨를 더 줄이지 않는다. 본문 기준 크기는 17px이고, 날짜·작성자 같은 메타 정보도
  0.88rem 아래로 내리지 않는다.

## 주의할 점

- `firebaseConfig`가 클라이언트에 노출되는 것은 정상이다.
  Firebase 웹 앱은 설정값을 공개 전제로 배포하며, 접근 제어는 Firestore 보안 규칙이 담당한다.
- **새 Firestore 컬렉션을 추가하면 Firebase 콘솔의 보안 규칙에도 반드시 추가해야 한다.**
  기본이 전부 차단이라 코드만 고치면 저장이 안 된다. 이 사실을 사용자에게 알린다.
- 현재 쓰는 컬렉션: `guestbook`, `board`, `ratings`, `ratings/{id}/comments`.
- 게시판·평점·방명록은 동작하는 기능이다. 다른 작업 중에 부수적으로 수정하지 않는다.
- 이름, 학교, 이메일 등 개인정보는 `OOO` 자리 표시자를 유지한다. 임의로 지어내지 않는다.

## 작업 방식

- 한 번에 한 가지 작업만 한다. 요청하지 않은 부분은 건드리지 않는다.
- 구조를 바꾸는 작업은 계획을 먼저 제시하고 승인을 받은 뒤 진행한다.

## 실행

빌드 도구 없음. `index.html`을 브라우저로 열거나 `python -m http.server`로 서빙한다.
